<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.ugwulo.firestorm_personal_fin_tracker">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/MaterialTheme">
        <activity android:name=".ui.EditWalletEntryActivity"></activity>
        <activity android:name=".baseClasses.BaseActivity" />
        <activity android:name=".ui.CircularRevealActivity" />
        <activity android:name=".ui.AddWalletEntryActivity" />
        <activity android:name=".options.OptionsActivity" />
        <activity android:name=".ui.MainActivity" />
        <activity android:name=".UserAuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>